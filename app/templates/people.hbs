{{#each model.people as |person|}}
	<p class="salutate">
		Hello 
		{{#if person.isFemale}}
			<strong>Miss.</strong>
		{{else}}
			<small>Mr.</small>
		{{/if}}
		{{#link-to 'people.person' person.id}}
			{{person.name}}
		{{/link-to}}
	</p>
{{/each}}

{{outlet}}